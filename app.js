let diceArray = [];
let contaiuner = document.createElement('div');
document.body.appendChild(container);

class Dice { 
    constructor (value){
        this.value = value;
        this.div = document.createElement('div');
    }
}
let dice = [];





function getRandomColor() {
    let randomColor = color[Math.floor(Math.random() * color.length)];
    return randomColor;
}
















